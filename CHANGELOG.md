# Changelog

All dates 2026. This log is deliberately honest about what broke; the full
defect ledger with cause -> fix -> invariant mapping is SPEC.md §B.

## 07-26
- **Benchmark v3 (drift)**: the temporal axis - the world changes between
  runs; does the assessment change with it, and at what cost? Pre-registered
  before running, like the two before it. First true-positive test of the
  demand stamp (buyer evidence finally arrives; it should CLEAR).
- Refute-a-claim issue template, CONTRIBUTING, this changelog.

## 07-25 (a long day)
- **Benchmark v2 published: second null.** The realistic weak-prompt baseline
  never fell for the demand trap - the stamp's value is machine-readability
  (6/6 vs 0/6), not skepticism the model already has. A scorer defect
  (penalising the ledger for citing sources) found, disclosed, and shipped as
  score3.py beside the untouched score2.py.
- **Benchmark v1 published: first null**, with the raw outputs. Structure
  separated (14 format failures vs 0); error-rate did not.
- **V14**: CI parses a committed artifact the run did not create, after B6
  proved self-tests can be self-consistently blind to encoding corruption.
- **Ponytail cut: -290 lines**, including the ported coding-agent compressors
  no research path ever produced.
- **Adversarial review (5 personas + independent judge): reshape, as-is
  3/10.** Install docs crashed the first command; free text cleared the
  "tool-enforced" stamp; the meter measured markdown, not spend; the evidence
  was gitignored. All fixed same day; log in SPEC §B B5.
- **Typed buyer evidence** (`buyer:reply|call|signature|payment`) enforced at
  write time; the stamp reframed honestly as tamper-EVIDENT, not a lie
  detector.
- MCP server (13 tools) with stdio tests in CI; CLI wrappers for every
  mutation; `ledger.py demo`; committed worked example; the public landscape
  thesis (`research/landscape.md`) with EviBound / POPPER / HDSO /
  ResearchLoop cited as prior art we went looking for.

## 07-24 (as the private predecessor, extracted 07-25)
- Living thesis ledger, recheck/diff loop, yield-beside-cost meter, claim
  classes with the derived demand stamp, crash-safe run manifest with
  redo-never-skip resume. Each earned by a logged defect (SPEC §B B1-B4).
